<Window x:Class="HelpMeChat.ReplySelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择回复" Height="400" Width="600" Topmost="True" ShowInTaskbar="False" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <!-- 主题配色 -->
        <SolidColorBrush x:Key="AccentBrush" Color="#10B981"/>
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderBrushColor" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextBrush" Color="#334155"/>

        <!-- 扁平按钮样式 -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 输入样式 -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Margin" Value="4,4,0,2"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- ComboBox 样式 -->
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- 卡片容器样式 -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0" Name="MainTabControl" Loaded="MainTabControl_Loaded">
            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="预设回复" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                </TabItem.Header>
                <StackPanel Orientation="Vertical">
                    <Label Content="选择预设回复" Grid.Row="0" Margin="0,0,0,0"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="ReplyComboBox" Grid.Column="0" SelectionChanged="ReplyComboBox_SelectionChanged"/>
                        <Button x:Name="ConfirmButton" Grid.Column="2" Click="ConfirmButton_Click" Cursor="Hand" MinWidth="50">
                            <Path Data="M2,8 L6,12 L14,2" Stroke="White" StrokeThickness="2" StrokeLineJoin="Round" Stretch="Uniform"/>
                        </Button>
                    </Grid>
                    <TextBlock x:Name="ValueTextBlock" Margin="4,6,0,0" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap"/>
                </StackPanel>
            </TabItem>
            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="AI 生成" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                </TabItem.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label Content="选择提示词" Grid.Row="0" Margin="0,0,0,0"/>
                    <ComboBox x:Name="PromptComboBox" Grid.Row="1" SelectionChanged="PromptComboBox_SelectionChanged"/>
                    <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                        <TextBox x:Name="AiResponseTextBox" Margin="0,6,0,0" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" Background="#F3F4F6" Padding="4" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                    </ScrollViewer>
                    <GridSplitter Grid.Row="4" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Center" Background="{StaticResource BorderBrushColor}" ResizeDirection="Rows"/>
                    <Grid Grid.Row="5" Margin="0,6,0,0" MinHeight="80">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="CustomPromptTextBox" Grid.Column="0" Margin="0,0,10,0" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" Background="#F3F4F6" Padding="4" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" PreviewKeyDown="CustomPromptTextBox_PreviewKeyDown"/>
                        <Button x:Name="GenerateAiButton" Grid.Column="1" Click="GenerateAiButton_Click" MinWidth="50">
                            <Path x:Name="PlayPausePath" Data="M 0 0 L 0 20 L 15 10 Z" Fill="White" Stretch="Uniform"/>
                        </Button>
                        <Button x:Name="ConfirmAiButton" Grid.Column="2" Click="ConfirmAiButton_Click" IsEnabled="False" Cursor="Hand" MinWidth="50">
                            <Path Data="M2,8 L6,12 L14,2" Stroke="White" StrokeThickness="2" StrokeLineJoin="Round" Stretch="Uniform"/>
                        </Button>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>